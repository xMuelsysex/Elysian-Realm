# Core

- Repository root: `D:/github/Elysian-Realm`.
- Current Unity prototype target for dialogue/font work: `unity/dorm-game`.
- Trellis is present under `.trellis/`; `python ./.trellis/scripts/get_context.py --mode packages` currently reports a single-repo project with `backend` and `frontend` spec layers, but no Unity-specific spec layer.
- Existing AGENTS/project rule: do not create commits, branches, deploy, or publish unless the user explicitly asks. The worktree may be dirty; preserve unrelated user changes.
- Dialogue font crispness plan lives at `docs/superpowers/plans/2026-06-15-dialogue-font-crispness.md` and is the active implementation plan for current work.